# =============================================================================
# Database
# =============================================================================

DATABASE_URL=postgresql://clubmanager:clubmanager@localhost:35432/clubmanager

# =============================================================================
# Redis
# =============================================================================

REDIS_URL=redis://localhost:36379

# =============================================================================
# S3-Compatible Object Storage
# =============================================================================
# Works with MinIO (local), AWS S3, or Cloudflare R2.
# S3_ENDPOINT: Internal URL used by the API server (server-to-S3).
# S3_PUBLIC_ENDPOINT: Public URL for presigned URLs (browser-to-S3).
#   Only needed when S3_ENDPOINT is not reachable from the browser
#   (e.g., Docker internal network). Falls back to S3_ENDPOINT if not set.
#
# --- MinIO (local development, devcontainer) ---
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_ENDPOINT=http://localhost:35900
S3_ACCESS_KEY_ID=clubmanager
S3_SECRET_ACCESS_KEY=clubmanager
S3_BUCKET=clubmanager
S3_FORCE_PATH_STYLE=true
#
# --- AWS S3 ---
# S3_ENDPOINT=https://s3.eu-central-1.amazonaws.com
# S3_ACCESS_KEY_ID=AKIA...
# S3_SECRET_ACCESS_KEY=...
# S3_BUCKET=clubmanager-prod
# S3_REGION=eu-central-1
# S3_FORCE_PATH_STYLE=false
#
# --- Cloudflare R2 ---
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# S3_ACCESS_KEY_ID=...
# S3_SECRET_ACCESS_KEY=...
# S3_BUCKET=clubmanager
# S3_REGION=auto
# S3_FORCE_PATH_STYLE=false

# =============================================================================
# Authentication (Better Auth)
# =============================================================================

# Better Auth secret (generate with: openssl rand -base64 32)
# REQUIRED for production - used to sign session cookies
BETTER_AUTH_SECRET=

# =============================================================================
# OAuth Providers (Optional)
# =============================================================================

# Google OAuth (https://console.cloud.google.com/apis/credentials)
# Implementation ready - just add credentials to enable
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# NEXT_PUBLIC_GOOGLE_OAUTH_ENABLED=true

# Microsoft OAuth (https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps)
# Requires minimal implementation first (see ticket #96)
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
# NEXT_PUBLIC_MICROSOFT_OAUTH_ENABLED=true

# =============================================================================
# Email / SMTP (Optional - uses Mailhog in dev)
# =============================================================================

SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_ADMIN_EMAIL=noreply@clubmanager.local

# =============================================================================
# AI (Document Recognition)
# =============================================================================

# OPENAI_API_KEY=

# =============================================================================
# App URLs
# =============================================================================

# Internal ports (devcontainer maps 33000:3000 and 33001:3001 externally)
WEB_PORT=3000
API_PORT=3001

# Public URL for OAuth callbacks and CORS
NEXT_PUBLIC_APP_URL=http://localhost:33000
API_URL=http://localhost:33001

# =============================================================================
# Cloudflare Turnstile CAPTCHA (Optional - enables bot protection)
# =============================================================================

# Get keys from: https://dash.cloudflare.com/ → Turnstile
# Leave empty to disable CAPTCHA (rate limiting still applies)
# TURNSTILE_SECRET_KEY=
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# =============================================================================
# Legal Pages (Optional - external hosting)
# =============================================================================

# Override internal legal pages with external URLs (e.g., marketing site)
# If not set, uses internal routes (/impressum, /datenschutz, /nutzungsbedingungen)
# NEXT_PUBLIC_IMPRESSUM_URL=https://example.com/impressum
# NEXT_PUBLIC_DATENSCHUTZ_URL=https://example.com/datenschutz
# NEXT_PUBLIC_NUTZUNGSBEDINGUNGEN_URL=https://example.com/nutzungsbedingungen

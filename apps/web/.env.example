# =============================================================================
# Next.js Auth.js Configuration
# =============================================================================

# Auth.js secret (generate with: npx auth secret)
AUTH_SECRET=

# GoTrue OIDC Configuration
GOTRUE_URL=http://localhost:39999
GOTRUE_CLIENT_ID=gotrue
# GOTRUE_JWT_SECRET is shared with docker-compose (same as GOTRUE_JWT_SECRET)

# Trust host for production (set to true behind reverse proxy)
AUTH_TRUST_HOST=false
